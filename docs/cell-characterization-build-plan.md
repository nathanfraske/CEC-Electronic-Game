<!-- SPDX-License-Identifier: Apache-2.0 -->
# CELL-DESIGN IMPLEMENTATION PLAN (workflow w5pp51trl)

## DETERMINISM VERDICT
SOUND — independently re-verified against crates/sim-core/src/lib.rs and the web layer; the golden 0xeaac_3764_99e4_fa24 cannot move under any phase P0a-P9 as written. Confirmed at lib.rs:7353-7404: snapshot_hash folds tick -> per-node (u8 net_level for NetClass::Digital nets, else f64 node_v.to_le_bytes) -> ELEM_DFF (ff_q, ff_clk_prev) -> ELEM_SAMPLER -> ELEM_COMPARATOR -> ELEM_BEHAVIORAL beh_state ([u32; BEH_STATE_WORDS] LE), strictly append-only, folding NO params and NO failed_elements. Verified constants: BEH_STATE_WORDS=16 (lib.rs:1029), BEH_LUT_MODE_SLOT=4 (lib.rs:1513), BEH_SUBTICK_RATE_SLOT == RATED_CURRENT_SLOT == 2 (lib.rs:2452/2484), BEH_SPEC.LUT.prog=4 / term=[0,5,4,6,7,1,2,3] / defWord=0x6666 (netlist.ts:513). flag_and_clamp_fails writes only failed_elements which is explicitly NOT hashed. Boundary classification at lib.rs:2207 (analog_touched[n] && digital_touched[n]) => a collapsed cell whose output meets an analog load is NetClass::Boundary and folds the f64 node_v, so reproducibility holds via the deterministic MNA solve, NOT the quantized level (D2 correct). A purely combinational prog-4 LUT advances no state — all 16 words stay zero, folding like an inert block, so a placed combinational LUT is golden-safe. The RC golden places no behavioral block (lib.rs comment), so flattenUserIcs early-returns (userIc.ts:556, the !any guard) and the behavioral fold is zero-byte — verified the flatten loop is live-recursive (MAX_DEPTH=24/MAX_INSTANCES=4096 at userIc.ts:582-583, a perf budget, NOT a one-layer rule). D1 (lib.rs:2452 is only the slot const, no derate code exists), D9 (params never folded, PARAM_STRIDE=8) confirmed. Two robustness gaps pinned as TESTS not assertions: (D7) a cross-instance snapshot_hash equality test for a PLACED prog-4 LUT stepped N ticks twice (once driving a pure-Digital net, once a Boundary/analog load) guards D2 — there is NO structural barrier today preventing snapshot_hash being called on a scratch Sim (sim-wasm exposes it only on Simulation.inner by convention), so (D8) the ScratchSim newtype that does not expose snapshot_hash is genuine hardening, landed in P6. Every phase is golden-safe-by-construction: doc-only P0a/P0b; web/UI/persistence P1-P5,P7-P9; render-only on a separate hash-isolated Sim P6. Tape-out (P3b) is pure web graph/role mutation — no sim-core, no hashed field, the golden places none.

## AUDIT SUMMARY
All five lenses are well-grounded and re-verified against live code; the critique's two new catches both hold and are folded in. (1) PLAN COVERAGE HOLE (blocker, confirmed): rg for tape.?out/promote across userIc.ts/userLibrary.ts/App.svelte returns only doc-comments about the EXISTING second-seal (which promotes a single IC to a variant FAMILY, userIc.ts:100/userIcFamilyTargets), not a subassembly->IC path. As originally written P1-P4 produced subassemblies (role='subassembly') with NO way to become board-placeable, so 'build the gates and place them on your board' was unreachable. The live code shows the fix has two prongs: (i) starter-gate templates (P2) are authored INSIDE a package die via freshDieGraph(frameTag) (dieEditor.ts:85), so Seal yields role='ic' DIRECTLY — the doc §4.10 gate-friendly path — needing NO promotion; (ii) free-form box-captured subassemblies (P4) DO need an explicit Tape-out. A new phase P3b implements Tape-out (map internal nets->package pins reusing userIcPartKind's index map at userIc.ts:197 and captureSeal's framePackage read at :737, set role='ic', reuse resealUserIc at :284 for re-package). The plan now states which entrypoints land board-placeable directly vs require Tape-out. (2) P2 CATEGORY ERROR (major, confirmed): netlist.ts:284-296 INV is a {vccPin:2,gndPin:3,extra:[{t:12,a:0,b:2,c:1...},{t:11,a:0,b:3,c:1...}]} FET-EXPANSION descriptor consumed by compositeInternals — NOT an authorable die GraphSnapshot; P2 is reworded to author FRESH die graphs (placed ELEM_NMOS=11/PMOS=12 + wires + die frame + pinNames + preset Component.pinTests so dieTestGraph at dieEditor.ts:204 injects VCC/GND/IN and the seeded die solves), using the INV topology as reference only, scoped as 'hand-build 3 die graphs + verify each solves.' (3) GRASPABILITY: P7 now surfaces the sweep as a LIVE steppable truth-table teaching panel (render the scratch Sim's per-vector state, light the conducting PUN/PDN path via logicInternal.drawGateInternal) instead of a silent offscreen extraction — the owner's 'see WHY a NAND is a NAND.' (4) C1 tempered: logicInternal.drawGateInternal (:89) is a topology RENDERER consuming a pre-lit DetailOpts; the doc §3.2 'no JS gate/LUT truth evaluator' stays TRUE, so Tier-1 reuses the RENDERER but the TS boolean evaluator is genuinely new load-bearing work (P5). (5) P0 doc-drift closed: P0 writes the §2.3 CellBehavior and §2.9 pin-role blocks to their FINAL P3/P7 shapes (PinRole gnd|vcc|in|out|clk; sig = FNV-1a over a canonical-ORDERED inner-graph serialization, citing CLAUDE.md rule #1). (6) P0 split into P0a (determinism/sim-core precision) + P0b (UX/structure/scale) since it bundled ~8 interdependent rewrites mislabeled as one sitting. Net: the END STATE and golden contract are right; the corrections are precision, dead-citation cleanup, the now-closed tape-out coverage hole, the P2 mis-scope, and honest scoping of the unbuilt UX on-ramp.

## DESIGN DELTA (fold into doc, P0a/P0b)

### D0: §4.5/§4.9 Tape-out / subassembly->IC promotion (NEW — the missing on-ramp)
**change:** Add the promotion mechanism the doc LOCKS as the only path from a bare subassembly to a board-placeable IC, which has ZERO backing code today (rg tape.?out/promote finds only doc-comments about the EXISTING variant second-seal, userIc.ts:100/userIcFamilyTargets/App.svelte:2322). Specify TWO prongs and state which entrypoints use which: (A) DIRECT — a cell AUTHORED IN A PACKAGE die (the starter-gate templates, seeded via freshDieGraph(frameTag) at dieEditor.ts:85, and any drill-in build) already has a pinout, so Seal yields role='ic' with NO promotion (the §4.10 gate-friendly path); these are the parts the owner most wants and they reach the board in one step. (B) TAPE-OUT — a free-form / box-captured subassembly (role='subassembly', no package yet) requires an explicit Tape-out that (1) lets the player pick a package archetype/pinCount, (2) maps each boundary/role-tagged internal net to a package pin index (reuse userIcPartKind's pad-index mapping at userIc.ts:197 and captureSeal's framePackage read at :737), (3) sets role='ic' and re-packages via resealUserIc (userIc.ts:284). State plainly: box-select (P4) produces subassemblies that are STUCK in 'My Subassemblies' until Tape-out runs — so Tape-out (P3b) MUST precede shipping P4's capture as a complete loop.
**rationale:** BLOCKER: without this the headline 'build the gates and place them on your board' is unreachable through the plan; the live code both confirms the gap and supplies the reuse points for the fix.

### D1: §4.10a Density has a cost — heat
**change:** Drop the claim that heat is 'already golden-safe and half-specced … density derates RATED_CURRENT_SLOT (lib.rs:2452), which feeds the existing FAIL mask.' lib.rs:2452 is only `const RATED_CURRENT_SLOT: usize = 2;` — a repo-wide search finds NO density/derate/dieScale code in sim-core. Replace with: 'heat would be modeled WEB-SIDE by lowering the Real-mode slot-2 rating a too-dense part installs (NEW web work, no sim-core change); golden-safe because slot 2 only FAIL-flags and failed_elements is not hashed (flag_and_clamp_fails, lib.rs:6803-6808).' Cite RATED_CURRENT_SLOT (2445-2452) only for the slot's contract. Mark the derate factor itself as NEW, not 'half-present.'
**rationale:** D1: the cited line implements nothing of the kind; leaving it makes the doc claim a mechanism exists where it is absent, undercutting the doc's 'every mechanism anchored to a real file:symbol' promise.

### D2: §6 feature-reproducibility + §5 honest-cost list
**change:** Distinguish pure-Digital signal nets (fold u8 net_level, out of the MNA) from Boundary signal nets (fold f64 node_v, stay in the dense MNA). A collapsed cell's output that meets ANY analog element (LED, pull-up, an analog next stage, a scope tap, a real resistor) is NetClass::Boundary (classify_nets, lib.rs:2207) and the hash folds the analog-solved f64 (lib.rs:7360), not the quantized level. Reproducibility holds because the f64 MNA solve is itself deterministic. Add to §5's cost list: 'a collapsed cell's outputs that meet analog loads remain Boundary nodes in the dense matrix,' alongside the supply-net cost.
**rationale:** D2: §6's 'level is a deterministic function of (word,inputs)' is narrower than the code; the stated REASON is wrong even though the conclusion (deterministic) is right, and it weakens §5's 'adds nothing to the matrix for its logic' framing.

### D3: §6 (new golden-safety bullet)
**change:** Add: 'Param slots are NOT hashed — snapshot_hash folds no params (lib.rs:7353-7404), PARAM_STRIDE=8 (lib.rs:2592) — so adding param slots or widening PARAM_STRIDE is golden-safe by itself; only widening beh_state (BEH_STATE_WORDS=16, lib.rs:1029) or adding a new hashed field touches the contract, and beh_state already folds append-only after the comparator block.'
**rationale:** D9: a real, verified strengthening that materially de-risks route 2.5b and any future per-element param the gate work wants.

### D4: §2.3 CellBehavior interface code block (write to FINAL shape)
**change:** P0 must write the fenced §2.3 CellBehavior TS block to the shape P7 implements VERBATIM (no later phase silently invalidating the spec): CellBehavior { prog: number; word: number; mode: number; sig: number } where sig is documented inline as 'FNV-1a over a canonical-ORDERED serialization of the inner graph (NOT JS object-key order — CLAUDE.md golden rule #1 forbids the std default hasher for any reproducible value)', content-addressed so identical leaves share one extracted word. Add the LUT-emission line: prog-4 LUT emits value=4 (BEH_SPEC.LUT, netlist.ts:513); mode rides Component.mode -> params[4] (BEH_LUT_MODE_SLOT, lib.rs:1513; default 0 = combinational); the sub-tick rate/rating rides params[2]. A wrong/zero value yields an inert block (default prog 0) — name this as a correctness trap. Add a P0 invariant: 'the §2.3/§2.9 code blocks are the contract P3/P5/P7 implement verbatim.'
**rationale:** D3 + cross-phase doc-drift: the doc is meant to be a trustworthy build spec; if P0 leaves the §2.3 block in a pre-P7 shape an isolated P7 agent ships TS that contradicts the spec.

### D5: §2.6 correctness guard
**change:** Add half a sentence: the INPUT receiver beh_level (hard 0.5·rail split, no X/forbidden band, lib.rs:1385-1386) and the OUTPUT verifier fam.quantize (family fractions CMOS 0.3/0.7, TTL 0.16/0.4, can yield X, lib.rs:6177-6180) are DISTINCT functions with different thresholds — the guard rejects any output that quantize places in the forbidden band even when inputs read cleanly.
**rationale:** D4: prevents a reader conflating the two quantizers; a cell can read an input high yet land its output in quantize's forbidden band, which the guard must reject.

### D6: §2.1 / §6.2 wording
**change:** Reword 'flattenUserIcs no-op … REGISTRY + FAMILIES are empty for it' to: 'the golden places no user-IC kind, so flattenUserIcs' membership scan (the !any guard, userIc.ts:556) finds nothing and returns the graph byte-identical — independent of what else is registered in the session.'
**rationale:** D6: the registries are not GLOBALLY empty (a session may register user ICs); the guarantee is that the golden circuit PLACES none.

### D7: §2.5 wide-cell frontier (split route 2.5a)
**change:** Split route 2.5a into (a1) STRUCTURAL mapping — walk the discrete graph, one LUT4 per gate (a ≤2-in gate IS a 16-bit word), wire each internal signal net to a digital node; NO function-level synthesis, just a graph->element-list lowering; THIS is the 8086 path. (a2) FUNCTION-level Shannon decomposition only for the rare case of collapsing a single >4-in but <~16-in combinational leaf that WAS swept. State the 8086 endgame rides (a1). Resolves the §2.9 tension: leaves are swept; the fabric is structural composition of swept leaves, no wide-function synthesis. No Shannon/BDD/synthesis code exists in the repo today.
**rationale:** SIM-CORE major: the doc conflates two disjoint syntheses under one 'Shannon-expand' label; §2.9 already established you cannot sweep a >16-input cell to a table, so the wide function is never available as a table — the fabric must be structural, a different pass than the sweep machinery.

### D8: §2.2 table + §2.6 + §2.9 (sequential ceiling)
**change:** State the real registered-LUT ceiling: prog-4 registered computes Q+=LUT(f,g,h,c) from EXTERNAL inputs only — the index does NOT include current Q (beh_lut_live_index, lib.rs:1539-1542). So only ≤4-input D-type next-state functions with NO self-state-dependence collapse to one registered LUT. A toggle/JK/gated-hold (Q+=f(inputs,Q)) needs Q as a 5th index bit: either feed Q back as one of ≤3 remaining external nets (a 2-element fabric: LUT + explicit feedback net) or push to route 2.5a. Add 'no self-state-dependence' to the Phase-B guard. Mandate the sequential SWEEP protocol: require a declared async reset/preset pin (promote §2.9's open sub-question to mandatory), drive reset to force a known Q, then per input-vector issue a clock edge and read Q+ post-edge; any cell whose state cannot be forced via reset/preset is refused to the cheap face.
**rationale:** SIM-CORE two majors: a 1-bit registered LUT cannot express a self-dependent state machine, and a purely combinational input sweep cannot extract a next-state table without forcing/observing internal state via a reset path.

### D9: §2.7 cycle-equivalence contract
**change:** Add: cycle-equivalence under a high sub-tick rate holds ONLY when the fabric's internal interconnect is ENTIRELY pure-Digital. run_digital_subticks re-solves only self.digital_rows (pure NetClass::Digital, lib.rs:6744-6747); a fabric net also touched by any analog element (a pull-up on an internal bus, a shared supply tap) is Boundary and FROZEN during sub-ticks, so deep cones will not settle within one tick regardless of sub-tick rate. The structural-lowering route (a1) must emit only digital interconnect nets to preserve this.
**rationale:** SIM-CORE minor: the §2.7 equivalence silently assumes every inter-LUT net is pure-Digital; an analog-touched interconnect loses sub-tick propagation.

### D10: §3.6 Tier-2 local solve
**change:** Add: the operating-point read shows correct combinational inner V/I (FET divider voltages) but FREEZES any inner SEQUENTIAL sub-element at its reset value — commit_sequential_digital_state runs only inside step() (lib.rs:6503), never at reset. Animating an opened cell that itself contains flops requires the stepped-sim variant (OQ 8.2), with the frame-time-deterministic step-budget caveat. Keeps the 'DC read is enough' claim honest for the flop-containing case.
**rationale:** SIM-CORE minor: a one-shot operating-point read silently loses inner sequential animation even at Tier-2.

### D11: §2.7a + §4.10a (cross-reference the slot-2 collision)
**change:** Reconcile explicitly: a sub-ticked behavioral leaf carries the sub-tick rate in slot 2 and therefore CANNOT also carry a density-derated current rating in slot 2 (BEH_SUBTICK_RATE_SLOT == RATED_CURRENT_SLOT == 2, lib.rs:2452/2484) — yet a dense fabric (the §4.10a target) is exactly the deep-cone case most likely to need a high sub-tick rate. Resolve by either (a) applying the heat-derate only to the cell's genuinely-analog leaves and supply nets, not its sub-ticked behavioral elements, or (b) promoting the already-queued slot move (TODOS.md:758 — move BEH_SUBTICK_RATE_SLOT off slot 2, append-and-default-off) to a PREREQUISITE for heat-on-dense-fabric. Cross-link §2.7a, §4.10a, and TODOS.md:758.
**rationale:** SIM-CORE + scale minor: §2.7a and §4.10a both reach for slot 2 on behavioral elements and are mutually exclusive on the same element.

### D12: §4.10 σ-composition math (three corrections + data-model home)
**change:** (1) State the identity: σ IS the existing per-level container scale s. If footprint_px = contentBboxCells·PITCH·σ, the content-extent CANCELS in s = bodyBoxPx/(contentBboxCells·PITCH) and s collapses to exactly σ; cumulativeScale becomes a clean ∏σ ladder with NO change to the scale-application path (userIcInternalsView.ts:288-295) — only the footprint that feeds bodyB/fitW/fitH changes. Drop '(extent×σ)/parent-cell' (dimensionally incoherent). (2) Correct the MAX_SCALE claim: MAX_SCALE=1000 bounds the CAMERA (world.scale.x, board.ts:1977/2124/2309), never cumulativeScale; cameraZoom is threaded UNCHANGED down every level, so depth N needs cameraZoom≈INTERNALS_ZOOM/σ^N (board.ts:164-169) — the budget does NOT reset per level, and a LOOSER σ costs drill-depth (hits the ceiling sooner). Give the real bound: max depth = floor(log(MAX_SCALE/INTERNALS_ZOOM)/log(1/σ)); tune σ (and/or per-depth INTERNALS_ZOOM / a higher MAX_SCALE) to hit ~4-5 tiers. (3) Pin-floor precedence: max(content×σ, perimeter-to-fit-pins) breaks the s≡σ identity and √(content) proportionality whenever the floor dominates; pick (i) accept it as a bounded documented deviation (cap pins-per-edge, note the meter is approximate for pin-dominated cells) or (ii) keep footprint=content×σ strictly and overflow pins onto a denser perimeter packing — and define precedence against the existing PITCH/1-cell floors (userIcInternalsView.ts:285-286). (4) Data-model home: store σ as an explicit per-definition field (UserIc.process?:{sigma:number} or a node-name enum), default absent=1.0=today's behavior; keep the SSI->ULSI badge DERIVED for labeling but decouple it from σ. (5) Use internals.bbox (the inner-parts extent, userIcInternalsView.ts:272-277), NOT dieEditor.dieBounds (the archetype walls), as the content-extent input, excluding die-frame pin cells so a 4-gate cell's extent reflects 4 gates. (6) Note σ retroactively makes the zoom meter PHYSICALLY HONEST: under fit-to-box the bar label already lies for differently-dense cells in the same package (zoomMeter.ts:13-25); σ fixes it because viewScale becomes ∏σ.
**rationale:** SCALE-DENSITY: three load-bearing math errors and a backwards MAX_SCALE claim, plus σ has no place to live in the data model and the doc conflates content-bbox with archetype-walls.

### D13: §3.2 / §3.8 / §7 Phase 5 (Tier-1 reuses logicInternal — tempered)
**change:** Reframe Tier-1 PRECISELY: logicInternal.drawGateInternal (logicInternal.ts:89) is a RENDERER that consumes a pre-lit DetailOpts (it takes already-computed high/low state and draws which PUN/PDN path is hot, FUNC map at :15) — it does NOT evaluate the boolean function. So Tier-1 reuses the shipped TOPOLOGY RENDERER (so it doesn't redraw CMOS pull-up/pull-down geometry) but the doc §3.2 claim 'there is no JS-side gate/LUT truth evaluator (gate_logic_level/LUT eval live only in Rust)' remains TRUE and is the LOAD-BEARING Tier-1 work — the TS boolean evaluator (gate codes + the ≤4-in LUT) is genuinely new (P5 builds it). State the pedagogical goal: characterization must NOT hide the transistor structure — the cheap behavioral face is for the SOLVE; zoom-to-open on a player's NAND must still surface its real series/parallel FET topology via the reused renderer.
**rationale:** COMPLETENESS C1 (tempered per critique): the doc reinvents the topology RENDERER (a real but MINOR reuse miss), not the EVALUATOR (which P5 correctly still builds); the original over-claimed C1 as 'the single most important gap.'

### D14: §2.6 + §7 (truth-table verification AND live steppable sweep panel)
**change:** Two coupled additions to P7. (a) Truth-table verification: after the sweep extracts the 16-bit word, let the player tag the cell's intended function (AND/OR/NAND/…), compare the extracted word against the canonical table derived from the engine's own GATE_AUX/gate_logic_level (lib.rs:5887; netlist.ts:192-203), and surface a pass/fail truth-table panel (green per matching combo, red per mismatch) — the §2.6 guard only checks characterizABILITY (two-level/stable/pure), never that the extracted table equals the INTENDED gate, so a broken 'NAND' that is actually AND characterizes cleanly to the wrong word and passes. Use the engine's gate semantics as a zero-new-oracle ground truth ('your transistor NAND does not match a NAND — check node X'). (b) LIVE sweep visualization: surface the sweep itself as a steppable teaching panel — show the FET cell, step through each input combination, light the conducting pull-up/pull-down path (reuse logicInternal.drawGateInternal's lit topology, the same renderer P5 wires), and fill the truth-table row-by-row as each output settles. This RENDERS the offscreen scratch Sim P7 already builds (render its per-vector state instead of discarding it) — turning 'extraction' into 'watch your transistors compute their truth table.' Cross-link the Lab Book BUILD/PREDICT grader (game-lux-and-lab-book.md:151).
**rationale:** COMPLETENESS C2 + the critique's graspability catch: the characterization sweep is the single most pedagogically powerful artifact of the pipeline (the owner's 'see WHY a NAND is a NAND'); hiding it as a silent offscreen extraction wastes the most teachable moment, and verification gives 'build a NAND' a win condition.

### D15: §4.3 / §4.6 (player-gate vs built-in relationship)
**change:** Resolve the three-way choice: recommended = player gates COEXIST as their own library rows but can be 'set as my NAND' so zoom-to-open on the built-in NAND opens the player's FET design (a teaching swap), OR a curriculum mode where the built-in gates are greyed until the player builds each from FETs. EITHER way, the captureSeal built-in-name-collision refusal (isReservedTag at userIc.ts:169, checked in captureSeal at :860; test userIc.variants.test.ts:360 'reserved-tag guard refuses a seal tag that collides with a built-in') must be revisited — it currently rejects a seal named 'NAND'/'R'/'GND', forbidding the exact naming the owner's 'rebuild all the gates' goal requires.
**rationale:** COMPLETENESS C5: the whole 'rebuild ALL the gates' goal is undefined without this, and today's isReservedTag actively blocks naming a gate after a built-in.

### D16: §7 (characterized-face-equals-FET-face CI test + D7)
**change:** Add to Phase 5/7: a vitest case that for each characterized gate runs the FET-face netlist and the behavioral-face netlist on two scratch Sims across all 2^k inputs and asserts identical committed output levels (and, for registered cells, identical Q after a clock edge), reusing the netlist.test.ts seal-equivalence idiom (netlist.test.ts:137/319). Also add the D7 cross-instance snapshot_hash test (same prog-4 LUT netlist stepped N ticks twice, equal hash — once driving a pure-Digital net, once an analog/Boundary load).
**rationale:** COMPLETENESS C3 + DETERMINISM D7: the §2.7 cycle-equivalence and the D2 Boundary reproducibility are asserted but untested; the repo already has the exact idiom.

### D17: §2.6a (characterization failure UX)
**change:** Add a failure-UX subsection: on guard failure report the specific reason per pin/combo (multi-level node N at inputs 0b1010; output never settled within the step budget; output depended on a non-clock edge -> likely a latch/oscillator), highlight the offending inner node in zoom-to-open, and offer the explicit fallback ('runs analog — costs more, still correct, still openable'). The §2.9 sweep already knows which combo/output failed. Precedent: captureSeal already refuses with a (thin) reason (board.ts:2887).
**rationale:** COMPLETENESS C4: for a teaching tool the failure path is as important as the success path; a silent 'stays expensive' is the worst outcome.

### D18: §2.3 / §2.6 (CellBehavior.sig lifecycle)
**change:** Define sig as an FNV-1a (or equivalent deterministic) hash over a canonical-ORDERED serialization of the inner graph — NOT JS object-key order (CLAUDE.md golden rule #1: never the std default hasher for a reproducible value) — and content-address it so identical leaves share one extracted word (an 8086 of thousands of identical NANDs sweeps once per distinct leaf). Specify: on reseal the stale behavior is dropped and re-extracted; placed instances pick up the new word at the next netlist BUILD (already non-golden because a user IC is placed), so no mid-session hash discontinuity.
**rationale:** COMPLETENESS C6 + SIM-CORE add: 'edit a gate' is undefined across the 50-instance/CPU case, and a content-addressed sig is both a determinism requirement and a real build-time dedup win.

### D19: §4.4 / §7 (library at CPU scale)
**change:** Add a 'library at scale' subsection: search/filter by name + function class, group-by-tier AND group-by-function, and a dependency/usage view (built from the flattenUserIcs descent userIcsForGraph already does, userIc.ts:309). The library is flat most-recently-added today (userLibrary.ts:104) with no folders/search. Flag the real risk: registerLibrary() registers EVERY row into PART_KINDS/REGISTRY/FAMILIES at startup (userLibrary.ts:187) — hundreds of nested-only subassemblies (role='subassembly') must NOT flood the board parts bin (the role filter), and the perf of registering hundreds of process-only kinds is unexamined.
**rationale:** COMPLETENESS C7: building all the gates then composing to an 8086 means hundreds of rows; the badge sort key alone is insufficient.

### D20: §2.2 / §7 (intermediate multi-output phase)
**change:** Add a phase between the 1-output Phase A and the high-risk Phase-C fabric: the trivial multi-output case = emit ONE prog-4 LUT PER output bit sharing the same ≤4 inputs (k≤4, m outputs -> m LUT elements, no synthesis pass). A half-adder is literally {SUM=XOR, CARRY=AND} = two LUTs; XOR is one LUT; full-adder/mux a handful. Run the sweep once, slice each output column into its own LUT. This is the 80% of 'all the gates' and needs none of the Phase-C Shannon risk.
**rationale:** COMPLETENESS C8: the all-or-nothing 1-output-vs-general-fabric split swallows the most pedagogically important multi-output primitives (XOR, half/full-adder).

### D21: §4.9 (pinout-preview affordance + UX honesty + captureRegion)
**change:** Specify a NEW captureRegion(graph, cellRect, pinDecls) DISTINCT from captureSeal (which is a BFS-from-frame connected-subgraph, userIc.ts:729): captured node set = parts whose anchor cell is in the box (NOT a BFS); a net is a PIN iff it has ≥1 wire endpoint inside AND ≥1 outside (boundary-crossing) or is wired to a board-level VCC/GND/source; document the policy for a wire clipped mid-run and a fully-internal isolated sub-circuit. Add a PINOUT PREVIEW step: on 'Make subassembly,' before sealing, highlight each boundary-crossing net at its crossing cell, let the player confirm/rename/role-tag/remove each, and show the count ('5 pins: VCC, GND, A, B, OUT'). Stop calling drill-in and extraction 'one back-end' — they are two front-ends producing the same (cell graph + pinout), but the capture mechanisms differ. Separate the THREE things the doc conflates: flatten/solve recursion (LIVE), zoom-to-open render recursion (LIVE), AUTHORING recursion via drill-in (NOT live — the editor is single-context, App.svelte:2378-2380; note the one-level editor is ENOUGH for gates, FETs at depth 1, so the recursive-editor work is OFF the gate critical path).
**rationale:** UX blocker UX-1 + UX-3 + UX-5: the flagship on-ramp has no backing code and is semantically conflated with captureSeal; silent inference wastes the pedagogy; the doc implies a recursive editor that does not exist.

### D22: §2.9 / §4.10 (real pin roles; final union gnd|vcc|in|out|clk)
**change:** Add a real per-pin SEMANTIC ROLE that persists into the cell: extend the test role union to the FINAL shape gnd|vcc|in|out|clk AND carry it through captureSeal into a new UserIc.pinRoles?:PinRole[] beside pinNames (web/persistence only, append-and-default, mirrors pinNames). Today PinTestRole = gnd|vcc|in only (graph.ts:211) and those are throwaway authoring stimuli NEVER sealed; pinNames is presentation-only (graph.ts:189). The role must distinguish 'observe' (OUT — do NOT inject a source) from 'drive' (IN/VCC), feeding §3.3a's Thevenin-vs-observe boundary. State explicitly that the test-stimulus role and the sealed semantic role are two different things today and must be unified. P0 writes the §2.9 pin-role table to THIS final union so P3 implements it verbatim.
**rationale:** UX major UX-2 + cross-phase doc-drift: every gate-grasp argument leans on a pin-role affordance that does not exist, and P0 must spec the final union so P3 doesn't silently widen it.

### D23: §4.10 (defer the free-form box for gates; reuse the package die geometry + carve the two Seal paths)
**change:** For v1, let subassemblies REUSE the existing fixed-package die geometry (a subassembly is a not-yet-taped-out cell that still occupies a package-shaped die) rather than a parallel free-form authoring surface — the die editor is built around a fixed package body (dieBounds from package, dieEditor.ts:129-151; seal maps pins by INDEX, userIc.ts:197-234). A free-form box is a second authoring geometry with its own pin-placement/routing/seal-mapping. Gates have standard pinouts, so the free-form box is OFF the gate critical path; if wanted later, lean toward the GRIDDED variant (OQ iii). Carve the two Seal paths EXPLICITLY (this is what unblocks the headline loop): authored-in-a-package -> Seal yields an IC DIRECTLY (the pinout already exists — the starter-gate templates and all drill-in builds); authored-as-a-free-form-box -> Seal yields a SUBASSEMBLY, and an explicit Tape-out (§4.5, the new P3b) assigns a package and flips role='ic'. Otherwise the design regresses today's one-step seal into a two-step ceremony for the exact parts the owner most wants.
**rationale:** UX major UX-6 + UX-9 + the blocker fix: the two-commit ceremony adds friction with no payoff for gates; carving the in-package-Seal-yields-IC path is precisely what lets starter gates reach the board without a promotion step.

### D24: §7 phase table (hard sequencing dependencies)
**change:** Two blocking dependency notes. (1) The global collapse (Phase 9) MUST NOT ship before the Tier-2 local-solve + local UserIcInternals builder (Phase 6): today a sealed gate flattens to FETs so its inner currents show (userIcInternalsView.ts:570-571); once collapse lands, a collapsed gate shows partCurrent=0 (no flatSink struct) until the local solve is built — shipping collapse first visibly REGRESSES 'see inside.' (2) Tape-out (Phase 3b) MUST land before box-select capture (Phase 4) is presented as a complete loop, else a box-captured subassembly is stuck in 'My Subassemblies' with no path to the board. Promote §3.8's 'only then' to 'Phase 9 depends on Phase 6' and add 'Phase 4 depends on Phase 3b' in the table.
**rationale:** UX major UX-10 + the blocker: an implementer who reorders P6/P9 makes every opened gate go dark; one who ships P4 without P3b strands every box-captured subassembly.

### D25: §2.8 / §8 (player-facing name) + §3.3/§3.7 (ScratchSim isolation)
**change:** (a) Pick a player-facing verb for characterize/collapse ('shrink to a chip' / 'bake' / 'simplify') distinct from the engine term, consistent with the Seal/Tape-out naming discipline. (b) Specify that solveCell's scratch Sim is never exposed to any hashing/replay path, is reset-then-read only (no free-running step), and ideally a distinct newtype / #[doc(hidden)] ScratchSim that does NOT expose snapshot_hash, so isolation is type-enforced — there is NO structural barrier today (sim-wasm exposes snapshot_hash only on Simulation.inner by convention); add to docs/determinism.md: 'only the global Simulation.inner is hashed; scratch/inspection Sims are render-only.'
**rationale:** COMPLETENESS C10 + DETERMINISM D8: the flagship mechanic is in raw engine language, and the scratch-Sim hash isolation rests on reviewer vigilance rather than a structural barrier — the critique independently re-confirmed D8 is genuine hardening, not cosmetic.

### D26: ADR 0005 / ADR 0006 (reconciliation)
**change:** ADR 0006: replace the 'exactly one layer of user nesting … No recursion' rule with 'depth bounded by MAX_DEPTH=24 / MAX_INSTANCES=4096 (a perf budget), not a one-layer model rule' — flattenUserIcs IS live-recursive (a depth loop over pending instances, userIc.ts:582-583); note AUTHORING recursion via drill-in is still single-context (App.svelte:2378-2380) and is a separate, off-the-gate-critical-path item. ADR 0005: add a companion note that characterize/collapse deliberately breaks 'seal-as-same-netlist' in the SOLVE (a collapsed cell is one ELEM_BEHAVIORAL standing in for the FETs) while the render path still shows the genuine circuit — an intentional, fidelity-gated exception, not a regression.
**rationale:** ADR drift: 0006's stale one-layer rule contradicts the live recursive flatten; 0005 needs the collapse-breaks-netlist companion so the fidelity exception is recorded, not discovered.

## PLAN PHASES

### P0a: Doc precision pass — determinism / sim-core corrections (doc-only)  [risk=low]
**goal:** Land the determinism and sim-core scoping corrections so the doc is a trustworthy build spec before any code: the D1 dead citation, the D2 Boundary fold, the D9 params-not-hashed strengthening, the route-2.5a structural-vs-Shannon split, the registered-LUT no-self-state ceiling, the mandatory-reset sequential protocol, the pure-Digital-interconnect requirement, the two-quantizer note, and the slot-2 collision. Independently landable and reviewable.
**changes:**
  - docs §4.10a: reword the heat mechanism (delete the lib.rs:2452 derate claim; mark the Real-mode slot-2 derate as NEW web work; cite flag_and_clamp_fails 6803-6808)
  - docs §6/§5: add the Digital-vs-Boundary fold distinction (D2: collapsed cell -> analog load -> Boundary net folds f64 node_v at lib.rs:7360, reproducible via the deterministic MNA solve) and the 'Boundary signal nets stay in the dense MNA' cost; add the params-not-hashed strengthening (D9: PARAM_STRIDE=8, only beh_state/new hashed fields touch the contract)
  - docs §2.5: split route 2.5a into (a1) structural one-LUT4-per-gate lowering (the 8086 path) vs (a2) function-level Shannon for a single >4-in-but-<16-in swept leaf
  - docs §2.2/§2.6/§2.9: registered-LUT ceiling (≤4 EXTERNAL inputs, NO self-state-dependence, lib.rs:1539-1542) added to the Phase-B guard; mandatory declared reset/preset pin for the sequential sweep protocol; pure-Digital inter-LUT interconnect required for sub-tick equivalence (lib.rs:6744-6747); the two-quantizer note (input beh_level 0.5·rail vs output fam.quantize)
  - docs §2.3/§2.8: the prog-4 value=4 / mode->params[4] / inert-on-zero authoring trap (BEH_LUT_MODE_SLOT lib.rs:1513; netlist.ts:513)
  - docs §3.6: Tier-2 DC read freezes inner sequential sub-elements at reset (commit_sequential_digital_state runs only in step(), lib.rs:6503)
  - docs §2.7a/§4.10a: cross-reference the slot-2 collision (BEH_SUBTICK_RATE_SLOT==RATED_CURRENT_SLOT==2) and TODOS.md:758 as a prerequisite for heat-on-dense-fabric
  - docs/adr/0005: add the companion note that characterize/collapse breaks seal-as-netlist in the SOLVE (one ELEM_BEHAVIORAL stands in) while the render path still shows the genuine circuit — an intentional fidelity-gated exception
**goldenSafety:** Doc + ADR markdown only; touches no .rs/.ts/.svelte and no golden circuit. There is no code path between a .md edit and the engine fold order (lib.rs:7353-7404), so snapshot_hash 0xeaac_3764_99e4_fa24 cannot move.
**verify:** none (doc-only); spot-check the corrected anchors resolve

### P0b: Doc precision pass — UX / structure / scale corrections + ADR 0006 (doc-only)  [risk=low]
**goal:** Land the UX, structure, and scale corrections to their FINAL shapes so no later phase silently invalidates the spec: the σ-composition math, the §2.3 CellBehavior + §2.9 pin-role blocks written to their P3/P7 implementations verbatim, the C1 temper, C2/C5, the box-select-is-unbuilt honesty, the Tape-out promotion mechanism, the Phase-dependency notes, and the ADR 0006 one-layer-rule retirement.
**changes:**
  - docs §4.10: rewrite the σ-composition math (σ IS s ⇒ cumulativeScale=∏σ, no change to the scale-application path userIcInternalsView.ts:288-295; MAX_SCALE=1000 bounds the CAMERA not cumulativeScale, board.ts:1977/164-169, looser σ COSTS drill-depth; pin-floor precedence; UserIc.process.sigma data-model home default 1.0; internals.bbox not dieBounds; σ as the zoom-meter honesty fix zoomMeter.ts:13-25)
  - docs §2.3: write the CellBehavior TS block to its FINAL P7 shape (prog/word/mode/sig; sig = FNV-1a over a canonical-ORDERED inner-graph serialization, citing CLAUDE.md rule #1, content-addressed). Add the P0 invariant 'the §2.3/§2.9 code blocks are the contract P3/P5/P7 implement verbatim'
  - docs §2.9: write the pin-role table to its FINAL union gnd|vcc|in|out|clk (the sealed UserIc.pinRoles P3 adds), distinct from the throwaway PinTestRole stimulus (graph.ts:211)
  - docs §3.2/§7: C1 TEMPERED — Tier-1 reuses logicInternal.drawGateInternal's topology RENDERER (:89/:15) but the §3.2 'no JS truth evaluator' claim stays TRUE and the TS boolean evaluator is the load-bearing new Tier-1 work (P5)
  - docs §2.6/§7: C2 — a truth-table-verification phase (compare extracted word vs gate_logic_level/GATE_AUX, lib.rs:5887) AND surface the sweep as a LIVE steppable panel rendering the scratch Sim's per-vector state; C4 failure-UX subsection
  - docs §4.3/§4.6: C5 — resolve player-gate-vs-built-in (coexist + 'set as my NAND' / curriculum-grey) and flag that isReservedTag (userIc.ts:169, captureSeal :860, test :360) currently refuses a built-in-name seal
  - docs §4.5/§4.9: the Tape-out / subassembly->IC PROMOTION mechanism (the BLOCKER fix) — two prongs: authored-in-a-package Seals directly to role='ic' (no promotion); free-form box-capture yields role='subassembly' requiring Tape-out (pick package, map nets->pin indices reusing userIcPartKind :197 + framePackage :737, resealUserIc :284). Plus the §4.9 honesty that box-select is unbuilt (captureSeal is a BFS, :729) needing a NEW captureRegion, and that pin roles IN/OUT/VCC/GND/CLK do not exist today
  - docs §7: the sequencing notes — 'Phase 9 (global collapse) depends on Phase 6 (local-solve builder)' and 'Phase 4 (box-capture) depends on Phase 3b (Tape-out)'
  - docs/adr/0006: replace the 'exactly one layer … No recursion' rule with 'depth bounded by MAX_DEPTH=24/MAX_INSTANCES=4096 (a perf budget)' — flattenUserIcs is live-recursive (userIc.ts:582-583); note drill-in authoring is still single-context (App.svelte:2378-2380), off the gate critical path
**goldenSafety:** Doc + ADR markdown only; no source, test, or golden artifact touched. snapshot_hash 0xeaac_3764_99e4_fa24 cannot move.
**verify:** none (doc-only); optionally pnpm -C web format if any fenced TS in the doc is reflowed

### P1: Subassembly role flag + 'My Subassemblies' bin filter  [risk=low]
**goal:** Make the subassembly-vs-IC vocabulary real with one optional field and a bin filter: a role='subassembly' cell is hidden from the board parts bin and offered only inside the die-editor place flow. The smallest structural step toward the gate pipeline.
**changes:**
  - web/src/lib/userIc.ts: add optional role?: 'ic' | 'subassembly' to interface UserIc (after pinNames); absent => 'ic' = today's behavior. Carry it through captureSeal's SealCapture and userIcPartKind (label/derivation unaffected)
  - web/src/lib/userLibrary.ts: add a role(entry): 'ic'|'subassembly' reader that reads entry.ic.role (the def addToLibrary clones at :132) — NOT a separate LibraryEntry.role field (clobbered on re-seal upsert). registerLibrary/registerEntry stay role-agnostic
  - web/src/App.svelte: split the libraryEntries().map at :1589-1593 into a My-ICs list (role!=='subassembly') and a My-Subassemblies list (role==='subassembly'); the board parts bin (:3757-3763) renders only My ICs; add a 'My Subassemblies' source inside the die-editor place flow only
  - web/src/App.svelte: default role on first Seal — package-authored cells default 'ic'; free-form/box-captured cells default 'subassembly' (set in P4)
**goldenSafety:** Web/UI/persistence only; role defaults to 'ic' so every existing save and the bin are byte-identical, and flattenUserIcs/resolveUserIc/REGISTRY stay role-agnostic (the bit is purely a bin filter). No sim-core, no wasm boundary, no hashed field.
**verify:** pnpm -C web format; pnpm run build:wasm; pnpm -C web check; pnpm -C web lint; pnpm -C web test

### P2: Starter gate templates (NAND2 / INV / NOR2) — hand-built FET die graphs  [risk=low]
**goal:** The smallest thing that makes the owner's goal TANGIBLE: 'New gate from template' seeds the EXISTING one-level die editor with a pre-wired FET pull-up/pull-down DIE GRAPH (VCC/GND/A/B/OUT named + test-stimuli preset) so it solves and switches immediately, then Seals via the existing flow (authored-in-a-package => role='ic' directly, NO promotion) and places from My ICs.
**changes:**
  - web/src/lib/circuits/ (new gate-template modules): hand-author FRESH die GraphSnapshot constants — placed ELEM_NMOS(11)/ELEM_PMOS(12) Component instances + wires + the die frame + pinNames set, and Component.pinTests PRESET (VCC/GND on the rails, IN on inputs) so dieTestGraph (dieEditor.ts:204) injects stimuli and the seeded die solves and switches. Use the CEC9002 INV composite TOPOLOGY (netlist.ts:284-296: PMOS drain=Y/source=VCC/gate=A, NMOS drain=Y/source=GND/gate=A) as a TOPOLOGY REFERENCE ONLY — that constant is a {vccPin,gndPin,extra} FET-expansion descriptor, NOT an authorable die graph, so author from scratch. NAND2 = series-NMOS/parallel-PMOS; NOR2 = parallel-NMOS/series-PMOS. Scope: hand-build 3 die graphs + verify each solves and switches.
  - web/src/lib/dieEditor.ts: a gateTemplate(kind, frameTag): GraphSnapshot lookup the editor seeds a drill with — reuse the freshDieGraph(frameTag) plumbing (dieEditor.ts:85, which already seeds the die frame for the package) and replace the blank inner graph with the template (frame id reconciled via findDieFrameId)
  - web/src/App.svelte: a 'New gate ▸ NAND2 / INV / NOR2' entry that drills into the die editor pre-seeded with the template (reuse the existing buildSelectedFrame/drill path); the player edits/observes, then Seal as today (yields role='ic')
  - (optional) wire each template to its docs/ui/parts refsheet for the teaching link
**goldenSafety:** Pure web data + UI: new GraphSnapshot constants (like web/src/lib/circuits/) and a template picker. Templates are placed only when the player chooses them; the golden circuit places none. No sim-core/wasm/hashed change.
**verify:** pnpm -C web format; pnpm run build:wasm; pnpm -C web check; pnpm -C web lint; pnpm -C web test (add a per-template 'die solves and switches' unit test)

### P3: Real pin roles + integration-tier badge (derived)  [risk=low]
**goal:** Persist machine-readable pin roles (IN/OUT/VCC/GND/CLK) into the sealed cell so characterization and the role-tag UX have something to read, and show the derived SSI->ULSI badge as a sort/group key. Both web-only, both prerequisites the later characterization sweep and Tape-out depend on.
**changes:**
  - web/src/lib/graph.ts: extend the role union to include 'out' and 'clk' for a SEMANTIC role (kept distinct from the throwaway PinTestRole stimulus); add a new sealed-through UserIc.pinRoles?: PinRole[] beside pinNames (graph.ts:189 pattern) — written to the FINAL union P0b specced
  - web/src/lib/userIc.ts: carry pinRoles through captureSeal (mirror pinNames capture); the role distinguishes observe (OUT) from drive (IN/VCC) for §3.3a and is read by P3b's Tape-out and P4's pinout preview
  - web/src/lib/userIc.ts: an integrationTier(def): 'SSI'|'MSI'|'LSI'|'VLSI'|'ULSI' helper that recursively tallies ELEM_NMOS/PMOS/GATE through def.graph + nested defs (sibling to graphShape, netlist.ts:1840) and maps the count to a game-scaled band
  - web/src/App.svelte: show the tier badge on My ICs / My Subassemblies rows; group/sort by it. Keep the badge DERIVED and decoupled from any future σ (the badge is the count label; σ is the stored process node)
**goldenSafety:** Web-only: pinRoles is an optional sealed-through field defaulting absent (append-and-default, pinNames pattern); the tier badge is a derived label never hashed and never crossing the wasm boundary. No sim-core, no golden change.
**verify:** pnpm -C web format; pnpm run build:wasm; pnpm -C web check; pnpm -C web lint; pnpm -C web test

### P3b: Tape-out — subassembly -> board-placeable IC promotion  [risk=medium]
**goal:** Close the BLOCKER coverage hole: implement the doc-LOCKED Tape-out that turns a bare role='subassembly' cell into a board-placeable role='ic' by choosing a package and mapping internal nets to package pins — the ONLY path for a free-form/box-captured subassembly to reach the board. (Authored-in-a-package gates from P2 already Seal directly to 'ic' and skip this.)
**changes:**
  - web/src/lib/userIc.ts: a tapeOut(def, package, pinAssignment): UserIc that takes a role='subassembly' def, a chosen package archetype/pinCount, and a map of internal-net -> package-pin-index (driven by the cell's P3 pinRoles / pinNames), produces the packaged die graph (reuse userIcPartKind's pad index mapping at :197 and framePackage at :737), sets role='ic', and re-registers via resealUserIc (:284) for the re-package case
  - web/src/App.svelte: a Tape-out UX on a My-Subassemblies row — pick package (validate pinCount >= pin count), assign each role-tagged pin to a pad (default by order, draggable), preview the resulting pinout, confirm => the cell moves from My Subassemblies to My ICs and becomes board-placeable
  - reuse P3 pinRoles for the pin labels/roles shown in the assignment step; revisit isReservedTag (userIc.ts:169) only as far as P0b's C5 resolution requires (naming a tape-out 'NAND' is the curriculum case)
**goldenSafety:** Pure web graph/role mutation + UI: tapeOut is graph->graph like captureSeal/resealUserIc, sets the role bit and a package, and re-registers a placeable kind. Nothing runs in sim-core or crosses the hashed boundary; the golden places no user IC. role still defaults 'ic' for untouched saves.
**verify:** pnpm -C web format; pnpm run build:wasm; pnpm -C web check; pnpm -C web lint; pnpm -C web test (add a tapeOut net->pin mapping + role-flip unit test; assert a taped-out cell flattens/solves identically to its pre-tape-out die)

### P4: Overworld box-select -> 'Make subassembly' (captureRegion + pinout preview)  [risk=medium]
**goal:** The owner's recommended easy on-ramp: build on the board, box-select a region, infer the pinout from boundary-crossing nets, preview/confirm/role-tag the pins, and Seal into a role='subassembly' cell — which Tape-out (P3b) then promotes to board-placeable. A NEW capture function distinct from captureSeal's BFS, with a teachable pinout-preview overlay.
**changes:**
  - web/src/lib/userIc.ts: NEW captureRegion(graph, cellRect, pinDecls) — captured node set = parts whose anchor cell is in cellRect (NOT a BFS); a net is a PIN iff it has ≥1 wire endpoint inside AND ≥1 outside, or is wired to a board-level VCC/GND/source; define the clipped-wire and fully-internal-isolated-subcircuit policies; decline (or use the subassembly form of) a packaged board IC in the selection
  - web/src/lib/board.ts: a 'Make subassembly' action that appears when the marquee selection has ≥1 part (the marquee already mutates this.selected at :732/3825-3853); on trigger, compute candidate pins and show a PINOUT PREVIEW overlay (highlight each boundary-crossing net at its crossing cell; let the player confirm/rename/role-tag/remove; show the count)
  - web/src/App.svelte: on confirm, call captureRegion + the existing Seal path (role defaults 'subassembly' for a free-form-box capture); the result lands in My Subassemblies and is promoted to the board via P3b Tape-out
  - reuse the P3 pinRoles field for the role tags chosen in the preview
**goldenSafety:** Web/UI only: a new capture function + an overlay + a Seal call. Nothing runs in sim-core or crosses the hashed boundary; the golden circuit is never box-captured. captureRegion is pure graph->graph like captureSeal.
**verify:** pnpm -C web format; pnpm run build:wasm; pnpm -C web check; pnpm -C web lint; pnpm -C web test (add a captureRegion boundary-net unit test mirroring userIc.variants.test.ts)

### P5: Tier-1 forward-eval inner telemetry (reuse logicInternal renderer + new TS evaluator)  [risk=low]
**goal:** Light up a player gate's inner topology cheaply at any depth: REUSE the shipped CMOS pull-up/pull-down RENDERER (logicInternal.drawGateInternal) for the geometry, and add the genuinely-new TS BOOLEAN EVALUATOR for the gate codes + the ≤4-in LUT — so an opened gate shows which path is hot from its pin levels, with no wasm change.
**changes:**
  - web/src/lib/logicInternal.ts: reuse drawGateInternal (:89) + the FUNC map (:15) as the Tier-1 teaching RENDERER (it consumes a pre-lit DetailOpts — it draws which PUN/PDN path is hot, it does NOT evaluate); add the ≤4-in/16-entry LUT topology case alongside the gate codes
  - web/src/lib/ (new tiny TS truth EVALUATOR — the load-bearing new work): port gate_logic_level's boolean function (lib.rs:5887, codes mirrored in netlist.ts:192-203) + beh_lut_bit's index lookup to TS, compute inner node levels from package-pin levels analytically, and feed those levels into the reused renderer
  - web/src/lib/userIcInternalsView.ts: wire the per-inner-wire level/current the :533 TODO needs from the forward-eval result into drawUserIcInternals / drawDetailMOSFET
  - add a CI cross-check: a vitest asserting the TS evaluator agrees with the Rust gate semantics for every gate code + a sample of LUT words, so the two copies cannot drift
**goldenSafety:** Pure web boolean + render: a TS truth evaluator and a render wiring, both render-only. No solver, no wasm boundary, no hashed state. The golden is untouched.
**verify:** pnpm -C web format; pnpm run build:wasm; pnpm -C web check; pnpm -C web lint; pnpm -C web test (add the TS-vs-Rust truth cross-check)

### P6: solveCell scratch-Sim + local UserIcInternals builder (Tier-2 DC read)  [risk=medium]
**goal:** Recover REAL inner V and per-FET I_D for the single focused cell via an on-zoom local DC operating-point solve on a separate, hash-isolated Sim, with a Thevenin pin boundary and a new local-struct builder — the prerequisite that MUST land before any global collapse (P9) so opened gates never go dark.
**changes:**
  - crates/sim-wasm/src/lib.rs: add a render-only solveCell on a SEPARATE pooled scratch Sim — a #[doc(hidden)] ScratchSim newtype that does NOT expose snapshot_hash (D8 structural isolation); install via the existing set_netlist_pefgh (:116) and read state()/element_currents() after the reset DC solve (inv_dt=0, lib.rs:4710-4722)
  - web/src/lib/ (new local UserIcInternals builder): re-run innerGraph.restore(def.graph) + the pin/node resolution (netlist.ts:1621-1735) against the LOCAL sub-Sim numbering (the collapsed case has no flatSink struct — netlist.ts:1614-1726 is FET-branch only)
  - web/src/lib/userIcInternalsView.ts: for the deepest viewProbe cell (:117/302-329), feed drawUserIcInternals the LOCAL node_v/element_currents + the locally-built struct; Thevenin boundary per §3.3a (drive receiver pins through the family gout; do NOT pin driver pins; release high-Z); note inner SEQUENTIAL sub-elements freeze at reset (DC read)
  - crates/sim-core/src/lib.rs (test): add the D7 cross-instance snapshot_hash regression test (same prog-4 LUT stepped N ticks twice, equal hash, once pure-Digital output, once analog/Boundary load)
  - docs/determinism.md: add 'only the global Simulation.inner is hashed; scratch/inspection Sims are render-only'
**goldenSafety:** The local solve runs on a SEPARATE ScratchSim whose node_v never touches the global instance; snapshot_hash folds only the global inner (lib.rs:7353+) and the newtype does not expose it. The DC read is memoryless (inv_dt=0) and side-effect-free. The golden places no user IC, so it is doubly insulated. The new wasm entry is render-only and never on the per-frame main boundary.
**verify:** cargo fmt --all -- --check; cargo clippy -p sim-core -p sim-protocol --all-targets -- -D warnings; cargo test -p sim-core -p sim-protocol (incl. the new cross-instance hash test); pnpm run build:wasm; pnpm -C web check; pnpm -C web lint; pnpm -C web build; pnpm -C web test

### P7: Combinational ≤4-in characterization sweep + correctness guard + LIVE truth-table panel  [risk=medium]
**goal:** The flagship: at Seal, sweep a small combinational cell into a 16-bit word, guard it (two-level-via-quantize / stable / pure-combinational), surface the sweep as a LIVE steppable truth-table teaching panel (render the scratch Sim's per-vector state, light the conducting path), and let the player verify it against the intended gate's canonical table. Stores UserIc.behavior; flatten emits one prog-4 LUT instead of FETs when fidelity='behavioral'.
**changes:**
  - web/src/lib/userIc.ts: add CellBehavior { prog, word, mode, sig } and optional UserIc.behavior (default absent) — to the FINAL §2.3 shape P0b specced; define sig as an FNV-1a hash over a canonical-ORDERED inner-graph serialization (NOT JS key order; CLAUDE.md rule #1) and content-address it to dedup identical leaves
  - web/src/lib/ (sweep harness): spin up an offscreen ScratchSim (P6), install the die netlist via set_netlist_pefgh, drive each of the 2^k input combos to the rails, solve, read commit_net_levels output; the GUARD verifies each output via the SAME fam.quantize commit_net_levels uses (lib.rs:6177-6180), not beh_level; refuse on multi-level/unstable/non-combinational with the C4 failure reasons
  - web/src/lib/ (LIVE sweep panel): render the scratch Sim's per-vector state — show the FET cell, step each input combination, light the conducting PUN/PDN path (reuse logicInternal.drawGateInternal, the same renderer P5 wires), fill the truth-table row-by-row as each output settles ('watch your transistors compute their truth table')
  - web/src/lib/ (truth-table verify, C2): let the player tag the intended function and compare the extracted word against the canonical table from gate_logic_level/GATE_AUX (lib.rs:5887, netlist.ts:192-203); surface a pass/fail panel ('your transistor NAND does not match a NAND — check node X')
  - web/src/lib/graph.ts: add optional Component.fidelity (default-off, like tier/variant/mode)
  - web/src/lib/netlist.ts (flattenUserIcs branch): when a cell has a valid behavior AND the instance fidelity is 'behavioral', emit ONE ELEM_BEHAVIORAL via BEH_SPEC.LUT.term (value=4, mode->params[4]) instead of inlining FETs; invalidate behavior on reseal when sig changes
  - tests: add the C3 characterized-face-equals-FET-face vitest (both faces on two scratch Sims across all 2^k inputs, equal committed output levels) reusing the netlist.test.ts idiom
**goldenSafety:** A combinational ≤4-in cell collapses to ONE ELEM_BEHAVIORAL prog-4 LUT (lib.rs:1070), already hashed; snapshot_hash folds beh_state append-only after the comparator block (lib.rs:7353-7404) and a combinational LUT folds an all-zero state block (word lives in aux). behavior/fidelity are opt-in web fields the golden never sets; flatten is a no-op for the golden (the !any guard, userIc.ts:556). Params are not hashed (D9). The sweep runs on a separate ScratchSim. Golden-safe by construction.
**verify:** cargo fmt/clippy/test -p sim-core -p sim-protocol; pnpm run build:wasm; pnpm -C web check; pnpm -C web lint; pnpm -C web build; pnpm -C web test (incl. the face-equivalence test)

### P8: Registered ≤4-in 1-bit (D-type) sequential + trivial multi-output (one LUT per bit)  [risk=medium]
**goal:** Extend characterization to the registered ≤4-in 1-bit D-type case (next-state table, clock fixed to terminal b) and the trivial multi-output case (m outputs sharing ≤4 inputs -> m LUTs), unlocking XOR / half-adder / full-adder without the high-risk fabric synthesis.
**changes:**
  - web/src/lib/ (sequential sweep): require a declared async reset/preset pin (mandatory per the corrected §2.9); drive reset to force a known Q, then per input-vector issue a clock edge and read Q+ post-edge; REFUSE any cell with self-state-dependence (toggle/JK) or no forceable reset; emit a registered prog-4 LUT (mode->params[4]>=1, beh_lut_step, lib.rs:1554) with clock mapped to terminal b
  - web/src/lib/ (multi-output, C8): run the combinational sweep once, slice each output column into its own prog-4 LUT element sharing the same ≤4 inputs (half-adder = {SUM=XOR, CARRY=AND} = 2 LUTs); no synthesis pass
  - web/src/lib/netlist.ts: flatten emits the registered LUT / the per-output LUT set on the behavioral branch
  - extend the guard + the C3 face-equivalence test to assert identical Q after a clock edge for registered cells
**goldenSafety:** Registered/multi-output LUTs are still prog-4 ELEM_BEHAVIORAL emitted only for opted-in cells; the registered Q folds into beh_state append-only (lib.rs:7391-7402) and the golden places none. No new sim-core element or hashed field. Golden-safe by construction.
**verify:** cargo fmt/clippy/test -p sim-core -p sim-protocol; pnpm run build:wasm; pnpm -C web check; pnpm -C web lint; pnpm -C web build; pnpm -C web test

### P9: Wide-cell structural fabric (route a1) + global digital-keep policy  [risk=high]
**goal:** The CPU-scale enabler: lower a wide already-characterized-leaf design STRUCTURALLY (one LUT4 per gate, internal signal nets -> pure-Digital nodes, supply wired to a coarse shared rail set), and let the global build keep characterized cells digital. Lands only after P6-P8 prove out (and depends on P6's local solve so collapsed cells never go dark).
**changes:**
  - web/src/lib/ (structural lowering, route a1 per the corrected §2.5): walk the discrete graph, map each ≤2-in gate to a 16-bit LUT word, wire each internal signal net to a digital node; emit only PURE-DIGITAL interconnect (no analog/Boundary inter-LUT nets, per the §2.7 correction); wire all leaf VCC/GND to a SHARED small rail-node set (coarse supply, per §5) so the analog matrix carries O(#rails), not O(#cells)
  - web/src/lib/netlist.ts / flattenUserIcs: the global policy that picks the behavioral/fabric face over FET-inline at scale; the global sub-tick-rate policy (§2.7) and its slot-2 rating trade (§2.7a) — gate heat-on-fabric behind the TODOS.md:758 slot move
  - (optional route b) crates/sim-core/src/lib.rs: a new wider BEH_PROG_* arm for a ≤16-word-state FSM, appended after the existing programs (append-and-default-off), if a moderate-state FSM justifies one element
  - verify the P6 local-solve composes with multi-level black-boxing (a gate inside a cell inside a CPU)
**goldenSafety:** Route a1 is pure web graph-lowering to existing prog-4 LUT elements — no sim-core change. A route-b wider program would be a new arm appended after programs 1-8, emitted only for opted-in cells (append-and-default-off), golden-absent. The global policy only changes which face is emitted for PLACED user ICs; the golden places none. Golden-safe by construction.
**verify:** cargo fmt/clippy/test -p sim-core -p sim-protocol (incl. any new program-arm test); pnpm run build:wasm; pnpm -C web check; pnpm -C web lint; pnpm -C web build; pnpm -C web test

## PHASE 1 DETAIL (P0a)

**title:** P0a — Doc precision pass: determinism / sim-core corrections (doc-only, golden frozen)
**files:** ['docs/cell-characterization-and-integration-hierarchy.md', 'docs/adr/0005-sealed-subcircuits-and-zoom.md', 'HANDOFFS.md']
**steps:**
  - Doc §4.10a: reword the heat paragraph — delete the assertion that density 'derates RATED_CURRENT_SLOT (lib.rs:2452), feeding the FAIL mask' as if it exists. Verified live: lib.rs:2452 is ONLY `const RATED_CURRENT_SLOT: usize = 2;` and a repo search finds no density/derate/dieScale code in sim-core. Replace with: heat is modeled WEB-SIDE by lowering the Real-mode slot-2 rating a too-dense part installs (NEW web work); golden-safe because slot 2 only FAIL-flags and failed_elements is not hashed (flag_and_clamp_fails, lib.rs:6803-6808). Cross-reference §2.7a and TODOS.md:758 (the slot-2 sub-tick move) as a prerequisite for heat-on-dense-fabric.
  - Doc §6 + §5: add the Digital-vs-Boundary fold distinction — a collapsed cell driving an analog load is a Boundary net (classify_nets: analog_touched[n] && digital_touched[n], lib.rs:2207) that folds f64 node_v (lib.rs:7360), so reproducibility holds because the f64 MNA solve is deterministic, NOT because the fold is the quantized level. Add 'Boundary signal nets stay in the dense MNA' to the §5 cost list. Add the §6 strengthening (D9): params are not hashed (snapshot_hash folds none, lib.rs:7353-7404; PARAM_STRIDE=8), so adding param slots / widening PARAM_STRIDE is golden-safe; only widening beh_state (BEH_STATE_WORDS=16, lib.rs:1029) or a new hashed field touches the contract.
  - Doc §2.5: split route 2.5a into (a1) STRUCTURAL one-LUT4-per-gate graph->element lowering (the 8086 path, a ≤2-in gate IS a 16-bit word) vs (a2) FUNCTION-level Shannon decomposition only for a single >4-in-but-<16-in combinational leaf that was swept. State the 8086 endgame rides a1; note no Shannon/BDD/synthesis code exists in the repo.
  - Doc §2.2/§2.6/§2.9: state the registered-LUT ceiling — prog-4 registered computes Q+=LUT(external inputs only); the index does NOT include current Q (beh_lut_live_index, lib.rs:1539-1542) — so only ≤4-input D-type next-state functions with NO self-state-dependence collapse to one registered LUT; add 'no self-state-dependence' to the Phase-B guard. Mandate the sequential SWEEP protocol: a declared async reset/preset pin is REQUIRED (promote §2.9's open sub-question to mandatory), drive reset to force a known Q, then per input-vector clock and read Q+; refuse any cell whose state cannot be forced. Add the §2.7 pure-Digital-interconnect requirement (run_digital_subticks re-solves only self.digital_rows, lib.rs:6744-6747; an analog-touched inter-LUT net is Boundary and frozen during sub-ticks). Add the two-quantizer note (input beh_level hard 0.5·rail, lib.rs:1385-1386, vs output fam.quantize family fractions, lib.rs:6177-6180).
  - Doc §2.3/§2.8: add the prog-4 LUT emission trap — emits value=4 (BEH_SPEC.LUT, netlist.ts:513); CellBehavior.mode rides Component.mode -> params[4] (BEH_LUT_MODE_SLOT, lib.rs:1513; default 0 = combinational); a wrong/zero value yields an inert block. Note programs 5-8 use other slots so a future wider prog must not assume slots 0/1/2 are the only behavioral params.
  - Doc §3.6: add that the Tier-2 DC read shows correct combinational inner V/I but FREEZES inner sequential sub-elements at reset (commit_sequential_digital_state runs only inside step(), lib.rs:6503), so animating a flop-containing opened cell needs the stepped-sim variant (OQ 8.2).
  - Doc §2.7a/§4.10a: cross-reference the slot-2 collision explicitly — a sub-ticked behavioral leaf carries the sub-tick rate in slot 2 (BEH_SUBTICK_RATE_SLOT == RATED_CURRENT_SLOT == 2, lib.rs:2452/2484) and therefore cannot also carry a density-derated rating in slot 2; resolve by deriving heat only on analog leaves/supply nets, or making TODOS.md:758 (move BEH_SUBTICK_RATE_SLOT off slot 2) a prerequisite for heat-on-dense-fabric.
  - docs/adr/0005: add a companion note that characterize/collapse deliberately breaks 'seal-as-same-netlist' in the SOLVE (a collapsed cell is one ELEM_BEHAVIORAL standing in for the FETs) while the render path still shows the genuine circuit (§3) — an intentional, fidelity-gated exception, not a regression.
  - Prepend a dated HANDOFFS.md section summarizing the P0a determinism/sim-core doc reconciliation and that the golden remains 0xeaac…fa24 (docs-only).
**tests:** None — this phase edits only markdown/ADRs and touches no source, test, or golden artifact. Verification is that no .rs/.ts/.svelte/.css file changed (so every gate command is a no-op) and the doc's cited line anchors resolve. Spot-checked LIVE this session and confirmed: lib.rs:2452 (RATED_CURRENT_SLOT const only), lib.rs:2484 (BEH_SUBTICK_RATE_SLOT = RATED_CURRENT_SLOT), lib.rs:2207 (Boundary classification), lib.rs:7353-7404 (fold order, no params/failed_elements), lib.rs:1513 (BEH_LUT_MODE_SLOT=4), lib.rs:1029 (BEH_STATE_WORDS=16), netlist.ts:513 (BEH_SPEC.LUT prog=4/defWord=0x6666), userIc.ts:582-583 (MAX_DEPTH=24/MAX_INSTANCES=4096, live-recursive flatten), userIc.ts:556 (the !any early-return), TODOS.md:758 (the slot-2 move). Optionally run pnpm -C web format if any fenced TS block in the doc was reflowed.
**goldenSafety:** This phase changes only documentation and ADR markdown. No Rust, no TypeScript, no Svelte, no golden circuit, no test fixture is touched, so cargo test -p sim-core's run_is_reproducible and the FNV-1a snapshot hash 0xeaac_3764_99e4_fa24 cannot move by construction — there is no code path between a .md edit and the engine's fold order (lib.rs:7353-7404, re-verified: tick -> per-node u8 net_level for Digital / f64 node_v otherwise -> DFF -> SAMPLER -> COMPARATOR -> beh_state, append-only, no params, no failed_elements). It is the safest possible first slice and lands cleanly behind the existing verification gate (every gate command is a no-op because no buildable file changed).